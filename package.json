{
  "name": "vernex-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev:pos": "npm --prefix apps/pos run dev",
    "install:apps": "npm --prefix apps/pos install",
    "typecheck": "npm --prefix apps/pos run typecheck",
    "lint": "npm --prefix apps/pos run lint",
    "build": "npm --prefix apps/pos run build"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
